spring:
  profiles:    active:    - dev 
  application:
    name: reminder-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
#      password: ${CONFIG_SERVICE_PASSWORD}
#      username: user